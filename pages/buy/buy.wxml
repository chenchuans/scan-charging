<!--pages/buy.wxml-->
<view class="list">
    <view class="item" wx:for="{{list}}" wx:key="{{index}}">
      <view class="floor">{{item.setname}}</view>
      <view class="desc">{{item.time}}</view>
    </view>
  </view>