<!--pages/userRecord.wxml-->
<view class="list">
    <view class="item" wx:for="{{list}}" wx:key="{{index}}">
      <view class="floor">{{item.time}}</view>
      <view class="desc">{{item.note}}</view>
    </view>
  </view>