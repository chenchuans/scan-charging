<!--pages/userRecord.wxml-->
<view class="list">
    <view class="item" wx:for="{{list}}" wx:key="{{index}}">
      <view class="floor">
        <view >{{item.time}}</view>
      </view>
      <view class="des">{{item.note}}</view>
    </view>
  </view>